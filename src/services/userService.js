/** LINE ユーザーの登録状態を管理する。 */
var UserService = {
  register: function (lineUserId) {
    // TODO: NotionClient を使ってユーザーを登録または再有効化する。
  },
  deactivate: function (lineUserId) {
    // TODO: NotionClient を使ってユーザーを無効化する。
  },
  listActive: function () {
    // TODO: 通知対象となる有効ユーザーを返す。
  }
};
