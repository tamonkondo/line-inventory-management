/** 在庫情報を LINE Flex Message として組み立てる。 */
var FlexBuilder = {
  buildInventoryCard: function (inventoryItem) {
    // TODO: 在庫カードの Flex Message オブジェクトを返す。
  },
  buildShortageMessage: function (inventoryItems) {
    // TODO: 不足在庫一覧の Flex Message オブジェクトを返す。
  }
};
