/** LINE Messaging API の薄いラッパー。 */
var LineClient = {
  reply: function (replyToken, messages) {
    // TODO: LINE Messaging API の reply エンドポイントを呼び出す。
  },
  push: function (userId, messages) {
    // TODO: LINE Messaging API の push エンドポイントを呼び出す。
  }
};
