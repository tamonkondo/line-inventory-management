# 実装書(15): セットアップ手順・E2E検証

- **依存**: 01〜14 すべて
- **対象**: コードではなく手順とチェックリスト(実施結果は本ファイルに追記してよい)

## 1. Notion側セットアップ

1. Notion Integrationを作成し、**Internal Integration Token** を控える。
2. `docs/notion-schema.md` の通りに3つのDBを作成する:
   - 在庫DB(Inventory): 品名(Title)/在庫あり(Checkbox)/カテゴリ(Select)/写真(Files & media)/購入先(Multi-select)/メモ(Text)/購入履歴(Relation→購入履歴DB)/最終購入日(Rollup: 購入履歴→購入日→Latest date もしくは Max)/更新者(Relation→ユーザーDB)
   - ユーザーDB(Users): 表示名(Title)/LINE User ID(Text)/ステータス(Select: 有効・無効)/登録日(Date)
   - 購入履歴DB(PurchaseHistory): 名前(Title)/対象品目(Relation→在庫DB)/購入日(Date)/購入先(Select)/記録者(Relation→ユーザーDB)
3. **3つのDBすべてをIntegrationに共有**(Connections に追加)する。忘れると404になる。
4. 各DBの**データソースID**を控える(DBを開く → データベース設定 → データソースを管理 → 「⋯」→ データソースIDをコピー)。データベースID(URLの32桁)は使わない。
5. **プロパティ名がコードの `NOTION_PROPS`(`src/config.ts`)と完全一致していることを確認**(全角/半角・スペースに注意)。
6. 在庫DBにテストデータを2〜3件入れる。
7. `docs/notion-schema.md` の「確定後に記載する項目」にデータソースIDを記入する。

### 検証ポイント(未確定事項の解消)

- [ ] **Rollup検証**: 購入履歴DBに手で1件作り、在庫DBの「最終購入日」に日付が出るか。出ない場合は要件11.5#1の代替案(在庫DBにDateプロパティ+GAS更新)へ切り替え、`notion-schema.md` と実装書08を更新する。
- [ ] **File Upload API検証**: 実装書04の `test_notionUpload` を実行し、`Notion-Version: 2026-03-11` で `/file_uploads` が動くことを確認する。

## 2. LINE側セットアップ

1. LINE Developersコンソールで Messaging APIチャネルを作成(既存の公式アカウントがあれば流用)。
2. **チャネルアクセストークン(長期)** と **チャネルシークレット** を控える。
3. 応答設定: あいさつメッセージOFF・応答メッセージOFF・Webhook ON。

## 3. ローカル環境と .env

1. `cp .env.example .env` を実行し、控えた実値を記入する(**.envはコミットしない**。gitignore済み)。
2. `.env` はローカルの控え兼セットアップ作業のチェックリストとして使う。**GASの実行時に読まれるのはスクリプトプロパティ**(§4-4で同じキー名で登録する)。

## 4. GASデプロイ

1. `npm install` → `npx clasp login`。
2. GASプロジェクトを作成し、`.clasp.json` を作る(git管理外):
   ```json
   { "scriptId": "<.envのCLASP_SCRIPT_IDの値>", "rootDir": "dist" }
   ```
3. `npm run typecheck` → `npm run push`(esbuildビルド+clasp push)でソース反映。
4. スクリプトプロパティを設定(`.env` と同じキー名・値):

| キー | 値 |
| --- | --- |
| `LINE_CHANNEL_ACCESS_TOKEN` | LINEチャネルアクセストークン |
| `LINE_CHANNEL_SECRET` | LINEチャネルシークレット |
| `NOTION_TOKEN` | Notion Integration Token |
| `NOTION_INVENTORY_DB_ID` | 在庫DBのデータソースID |
| `NOTION_USERS_DB_ID` | ユーザーDBのデータソースID |
| `NOTION_PURCHASES_DB_ID` | 購入履歴DBのデータソースID |
| `RICHMENU_IMAGE_FILE_ID` | リッチメニュー画像のDriveファイルID |

(`CLASP_SCRIPT_ID` は `.clasp.json` 用なのでスクリプトプロパティには不要)

5. 「デプロイ」→「ウェブアプリ」: 実行ユーザー=自分 / アクセス=全員(匿名)。URLを控える。
6. LINE DevelopersのWebhook URLに設定し「検証」ボタンで疎通確認 → 200。
7. `setupRichMenu()` を実行(実装書14)。

> 注意: コード更新後は `npm run push` だけでなく、**デプロイの更新**(同じデプロイIDの新バージョン発行)が必要。URLが変わらないよう「デプロイを管理」から編集すること。

## 5. E2Eチェックリスト

前提: LINEアカウント2つ(A=自分、B=家族)でBotを友だち追加済み。

| # | 操作(Aの端末) | 期待結果 |
| --- | --- | --- |
| 1 | Botを友だち追加 | あいさつが返る。ユーザーDBに「有効」で登録される |
| 2 | リッチメニュー「在庫一覧」 | 全品目がFlexで表示。✅/❌が正しい |
| 3 | 「新規 トイレットペーパー」 | 登録完了+品目カード+写真の案内 |
| 4 | 続けて写真を送る | 「写真を登録しました」。Notionにファイルが付き、一覧にサムネイル |
| 5 | 「新規 トイレットペーパー」(再) | 「すでに登録されています」 |
| 6 | 「なくなった トイレットペーパー」 | 在庫切れ確認が返る。**Bの端末に通知が届く**。Aには通知が来ない |
| 7 | リッチメニュー「不足一覧」 | トイレットペーパーだけが表示される |
| 8 | 「なくなった トイレットペーパー」(再) | 「すでに在庫切れです」。通知は飛ばない |
| 9 | リッチメニュー「買った」→ 品目タップ | 在庫ありに戻る+「購入履歴に記録しました」 |
| 10 | 「履歴 トイレットペーパー」 | 今日の日付の履歴が出る。Notionの最終購入日も今日になる |
| 11 | 「編集 トイレットペーパー」→ 購入先を変える →「スーパー / Amazon」送信 | Notionの購入先Multi-selectに2つ付く |
| 12 | 「編集」→ 名前を変える → 既存品目名を送信 | 「すでにあります」でセッション継続。別名なら成功 |
| 13 | フロー途中で「キャンセル」 | 「キャンセルしました」で中断 |
| 14 | 「こんにちは」 | フォールバック(ヘルプ誘導)が返る |
| 15 | Notion画面で品目名を直接変更 → LINEで「在庫」 | 変更後の名前で表示される(Notion編集が反映: R-05) |
| 16 | Bの端末でBotをブロック | ユーザーDBのBが「無効」になる |
| 17 | ブロック中にAが「なくなった」 | Bに通知が飛ばない(エラーにもならない) |
| 18 | Bがブロック解除(再フォロー) | Bが「有効」に戻る。行は増えない |
| 19 | 5分放置したセッション(新規登録の品名待ち)にテキスト送信 | 通常コマンドとして処理される(セッション失効) |
| 20 | スタンプを送る | 無反応(エラーが出ない) |
| 21 | Aが写真付き品目を「なくなった」 | Bにテキスト+写真の通知が届く(実装書16) |
| 22 | Aがリッチメニュー「買った」→品目選択 | Bに「【補充】...」が届く。Aには届かない(実装書16) |
| 23 | 「新規登録」→品名を送信 | 登録完了+**Notionの編集ページURL**が返り、リンクからページが開ける(R-14) |
| 24 | 品目カードの「編集(Notion)」ボタンをタップ | Notionの該当ページが直接開く(R-14) |
| 25 | 「編集 品名」と送る | NotionページのURLがテキストで返る(R-14) |

## 6. 完了報告

- 全チェックが通ったら、この表の結果(パス/フェイル)と発見事項を記録し、`docs/requirements.md` の11.5未確定事項を確定へ更新する。
- 最終確認: `grep -rn "var " src/` が0件、`npm run typecheck` がクリーンであること。

---

## 7. 実施状況(2026-07-10 記入)

### 自動検証(完了)

- [x] `npm run typecheck` クリーン
- [x] `npm run build` 成功(`dist/main.js` にGAS用トップレベル関数が生成されることを確認)
- [x] ユニットテスト 136件 全パス(`npm test`)
- [x] `src/` に `var` 宣言なし・旧 `.js` 雛形なし
- [x] コードレビュー(8角度)実施、指摘を修正済み(コミット `d9051de` 参照)

### 手動セットアップ・実機E2E(未実施 — 実クレデンシャルが必要)

- [ ] §1 Notion DB作成+Integration共有+DB ID記入
- [ ] §1 検証ポイント: Rollup動作 / File Upload APIのバージョン互換
- [ ] §2 LINEチャネル設定
- [ ] §3-4 `.env` 作成・スクリプトプロパティ登録・デプロイ・Webhook疎通
- [ ] §5 E2Eチェックリスト20項目

### 既知の制限(実装時に確定)

1. **署名検証**: GASはWebhookのHTTPヘッダを読めないため、X-Line-Signatureの完全検証は不可(構造検証+URL秘匿で代替。`src/utils/signature.ts` 参照)。強化するならデプロイURLに秘密トークンのクエリパラメータを付け `e.parameter` で照合する方式が追加可能。
2. **写真URLの失効**: Notionにアップロードした写真のURLは約1時間で失効する署名付きURL。送信直後のFlexカードでは表示されるが、古いトーク履歴を遡ると画像が切れて見えることがある(次回の一覧・カード表示では再取得されるため実用上の支障は小さい)。
